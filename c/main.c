#include "light.h"
#include "ray.h"

int main()
{
	vec src = {1, 2, 3};
	
	// t_light *l = init_light(src, 0.5, 0.5);
	// printv("light", l->pos);

	// t_ray *ray = init_ray(src);
	// printv("ray", ray->origin);
	// printv("ray", ray->dir);

	
}